using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

namespace MJM.HG
{
    //public class MainMenuCoroutine : MonoBehaviour
    //{
    //    public void SetSlider(int target, Slider slider)
    //    {
    //        StartCoroutine(LerpSlider(target, slider));
    //    }

    //    public IEnumerator LerpSlider(int target, Slider slider)
    //    {
    //        float timer = 0;
    //        float duration = 15;

    //        float start = slider.value;

    //        while (timer <= duration)
    //        {
    //            timer += 1;

    //            float newPosition = Mathf.Lerp(start, target, timer / duration);

    //            slider.value = newPosition;

    //            yield return new WaitForSeconds(1);
    //        }


    //    }
    //}
}
